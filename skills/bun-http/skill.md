---
name: bun-http
description: Bun native HTTP server and WebSocket patterns. Use when creating HTTP servers, handling routes, upgrading WebSocket connections, or working with Bun.serve() API. Do NOT use Hono — use Bun.serve() directly.
allowed-tools: Read, Grep, Glob
---

Read and follow the guidelines in `~/code/agent-context/bun-http.md`. Use glob, grep, read, and list operations to research the locations specified there.
